# ============================================================
# Ollama Proxy Server - Bootstrap Configuration
# ============================================================
# These settings are read from the .env file at startup.
# Copy this file to .env and customize values as needed.
# For Docker deployments, use: docker run --env-file .env ...
# ============================================================

# --- CORE SETTINGS ---
# Database URL (default: SQLite in current directory)
# Examples:
#   SQLite (default): sqlite+aiosqlite:///./ollama_proxy.db
#   PostgreSQL: postgresql+asyncpg://user:password@localhost/ollama_proxy
DATABASE_URL=sqlite+aiosqlite:///./ollama_proxy.db

# Admin user credentials (used for first login)
ADMIN_USER=admin
ADMIN_PASSWORD=changeme

# Server port (default: 8080)
PROXY_PORT=8080

# Secret key for session encryption (generate a new one for production)
# You can generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dd2a57833f4a2115b02644c3c332822d5b6e405d542a2258c422fb39a8e97b10

# Logging level (debug, info, warning, error, critical)
LOG_LEVEL=info

# --- BRANDING CONFIGURATION ---
# Customize the GUI appearance to match your branding

# Title displayed in the sidebar and footer
BRANDING_TITLE=Ollama Proxy

# Logo URL (optional) - provide a full URL to an image
# Example: https://example.com/logo.png
BRANDING_LOGO_URL=

# Show logo in sidebar header (true/false)
BRANDING_SHOW_LOGO=false

# Sidebar background color (Tailwind CSS color classes)
# Default: bg-gray-800 (dark gray)
# 
# Popular options:
#   bg-gray-800     - Dark gray (default)
#   bg-gray-900     - Darker gray
#   bg-blue-900     - Dark blue
#   bg-indigo-900   - Dark indigo
#   bg-purple-900   - Dark purple
#   bg-slate-800    - Dark slate
#   bg-zinc-800     - Dark zinc
#   bg-stone-800    - Dark stone
#   bg-red-900      - Dark red
#   bg-green-900    - Dark green
#   bg-amber-900    - Dark amber
#
# NOTE: For best results, use dark background colors with sufficient contrast.
#       The sidebar text is white, so ensure good readability.
BRANDING_SIDEBAR_BG_COLOR=bg-gray-800
